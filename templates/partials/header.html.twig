<header>
    <nav>
        <a href="{{ site.baseUrl }}">Home</a>
        {% for page in navigation_pages %}
            <a href="{{ site.baseUrl }}{{ page.slug }}.html">{{ page.title }}</a>
        {% endfor %}
        <a href="{{ site.baseUrl }}tags.html">Tags</a>
    </nav>
</header>